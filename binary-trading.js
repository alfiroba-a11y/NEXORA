const bt$=selector=>document.querySelector(selector);let openPositions=[];
function activeButton(position){return position?.type==='Even'?bt$('#even'):position?.type==='Odd'?bt$('#odd'):null}
function restoreButtons(){['even','odd'].forEach(id=>{const button=bt$(`#${id}`);button.classList.remove('contract-running');button.querySelector('.contract-status')?.remove();});}
function renderActive(){restoreButtons();const position=openPositions[0];if(!position)return;const button=activeButton(position);if(!button)return;const seconds=Math.max(0,Math.ceil((position.endsAt-Date.now())/1000));button.classList.add('contract-running');button.insertAdjacentHTML('beforeend',`<span class="contract-status">Running ${seconds}s <button class="inline-stop" data-stop="${position.id}">Stop</button></span>`);button.querySelector('.inline-stop').onclick=event=>{event.stopPropagation();stopContract(position.id)};}
function settle(position){if(!openPositions.includes(position))return;const won=Math.random()>.48;openPositions=openPositions.filter(item=>item!==position);if(won){window.nexoraBinaryWallet.credit(position.stake*1.952);toast(`${position.type} Demo contract won. Profit credited.`)}else toast(`${position.type} Demo contract lost.`);renderActive();}
function stopContract(id){const position=openPositions.find(item=>item.id===id);if(!position)return;clearTimeout(position.timer);openPositions=openPositions.filter(item=>item!==position);const cashout=Number((position.stake*.85).toFixed(2));window.nexoraBinaryWallet.credit(cashout);toast(`${position.type} Demo contract stopped. $${cashout.toFixed(2)} returned.`);renderActive();}
function trade(type){const stake=Number(bt$('#stake').value),wallet=window.nexoraBinaryWallet;if(!wallet)return toast('Wallet is loading.');if(!Number.isFinite(stake)||stake<1)return toast('Minimum stake is $1.');if(wallet.mode==='real'){return wallet.balance<stake?toast('Insufficient balance to trade. Deposit funds to your Real account.'):toast('Real Binary execution needs your licensed provider connection.');}const current=Number(localStorage.getItem('nexora_demo_balance')||10000),dailyLimit=Number(localStorage.getItem('nexora_daily_limit')||0),stakedToday=Number(localStorage.getItem('nexora_daily_staked')||0);if(dailyLimit&&stakedToday+stake>dailyLimit)return toast(`Daily Demo stake limit is $${dailyLimit.toFixed(2)}.`);if(stake>current)return toast('Demo balance is insufficient. Refresh Demo in Wallet.');wallet.debit(stake);localStorage.setItem('nexora_daily_staked',String(stakedToday+stake));const position={id:crypto.randomUUID(),type,stake,endsAt:Date.now()+12000};position.timer=setTimeout(()=>settle(position),12000);openPositions=[position];renderActive();toast(`${type} Demo contract opened for $${stake.toFixed(2)}.`);}
bt$('#even').onclick=event=>{if(event.target.closest('.inline-stop'))return;trade('Even')};bt$('#odd').onclick=event=>{if(event.target.closest('.inline-stop'))return;trade('Odd')};document.querySelector('#bottomPositions').onclick=()=>{bt$('#modalTitle').textContent='Positions';bt$('#modalText').innerHTML=openPositions.length?`<p><b>${openPositions[0].type}</b> · $${openPositions[0].stake.toFixed(2)}<br><small>Running · ${Math.max(0,Math.ceil((openPositions[0].endsAt-Date.now())/1000))} seconds remaining</small></p>`:'<p>No active Demo contracts.</p>';bt$('#modalAction').textContent='Close';bt$('#modalAction').onclick=()=>bt$('.x').click();bt$('#shade').classList.add('open');bt$('#binModal').classList.add('open')};document.querySelector('#bottomAi').onclick=()=>{const markets=['Volatility 10 (1s)','Volatility 25 (1s)','Volatility 50 (1s)','Volatility 75 (1s)','Volatility 100 (1s)'].map(name=>({name,score:(4+Math.random()*5.8).toFixed(1)})).sort((a,b)=>b.score-a.score);bt$('#modalTitle').textContent='NEXORA AI market activity';bt$('#modalText').innerHTML=`<p><b>${markets[0].name}</b> has the highest current activity score: ${markets[0].score}/10.</p><p class="housika-note">Activity is not a prediction and is not investment advice.</p>`;bt$('#modalAction').textContent='Select market';bt$('#modalAction').onclick=()=>{bt$('#instrumentName').textContent=markets[0].name;bt$('.x').click();toast('Market selected.')};bt$('#shade').classList.add('open');bt$('#binModal').classList.add('open')};setInterval(renderActive,500);
