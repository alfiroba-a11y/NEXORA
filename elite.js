const el$=s=>document.querySelector(s);document.querySelector('.engine-switch')?.remove();el$('.contract-fields').insertAdjacentHTML('afterbegin','<div class="digital-choice"><button class="active" data-choice="higher"><b>Higher</b><small>Exit above entry</small></button><button data-choice="lower"><b>Lower</b><small>Exit below entry</small></button></div><div class="digital-timer">Contract duration <b>10 seconds</b></div>');let outcome='higher';document.querySelectorAll('.digital-choice button').forEach(b=>b.onclick=()=>{outcome=b.dataset.choice;document.querySelectorAll('.digital-choice button').forEach(x=>x.classList.toggle('active',x===b));el$('#placeOrder').innerHTML=`Choose ${outcome==='higher'?'Higher':'Lower'} <span>→</span>`});const refresh=document.createElement('button');refresh.className='demo-reset';refresh.textContent='↻ Refresh demo balance to $10,000';el$('.balance-row').after(refresh);function wallet(){el$('#modalBackdrop').classList.add('open');el$('#fundModal').classList.add('open');el$('#fundContent').innerHTML='<h2>Wallet</h2><p>Manage deposits and withdrawals in one secure place.</p><div class="wallet-choice"><button id="wWithdraw">Withdraw</button><button id="wDeposit">+ Deposit</button></div>';el$('#wDeposit').onclick=()=>openModal('deposit');el$('#wWithdraw').onclick=()=>openModal('withdraw')}document.querySelector('.top-actions').insertAdjacentHTML('beforeend','<button class="wallet-top" id="walletTop">Wallet</button>');el$('#walletTop').onclick=wallet;el$('#cashierNav').onclick=e=>{e.preventDefault();wallet()};function setMode(mode){if(mode==='real'&&!localStorage.getItem('nexora_token')){el$('#modalBackdrop').classList.add('open');el$('#authModal').classList.add('open');showToast('Sign in or create an account to use Real funds.');return}activeAccount=mode;localStorage.setItem('nexora_mode',mode);document.querySelectorAll('.account-pill,.mobile-mode').forEach(x=>x.classList.toggle('active',x.dataset.account===mode));refresh.classList.toggle('show',mode==='demo');updateUI()}document.querySelectorAll('.account-pill,.mobile-mode').forEach(b=>b.onclick=()=>setMode(b.dataset.account));refresh.onclick=()=>{balances.demo=10000;updateUI();showToast('Demo balance refreshed to $10,000.')};setMode(localStorage.getItem('nexora_mode')||'demo');el$('#placeOrder').onclick=()=>{const stake=Number(el$('#amount').value);if(activeAccount==='real')return showToast('Live execution needs your licensed provider connection.');if(!stake||balances.demo<stake)return showToast('Insufficient demo balance.');balances.demo-=stake;const entry=last;updateUI();el$('#placeOrder').disabled=true;let seconds=10;const job=setInterval(()=>{seconds--;el$('#placeOrder').textContent=`Contract active · ${seconds}s`;if(!seconds){clearInterval(job);el$('#placeOrder').disabled=false;const won=outcome==='higher'?last>entry:last<entry;if(won){balances.demo+=stake*1.85;showToast(`Contract won — ${money(stake*.85)} profit credited.`)}else showToast(`Contract lost — ${money(stake)} stake deducted.`);updateUI();el$('#placeOrder').innerHTML=`Choose ${outcome==='higher'?'Higher':'Lower'} <span>→</span>`}},1000)};
